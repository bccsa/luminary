node_modules
.DS_Store
*.bkp

stats.html

# graphify knowledge-graph output (generated locally via `graphify`; not committed for now)
graphify-out/

# Claude Code agent worktrees (transient; must never be committed)
.claude/worktrees/
