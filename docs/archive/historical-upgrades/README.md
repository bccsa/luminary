# Historical schema upgrades

When a schema upgrade file is retired from [`api/src/db/schemaUpgrade/`](../../../api/src/db/schemaUpgrade/), move it here for reference instead of deleting it.

This folder is intentionally empty until the first upgrade is archived.
