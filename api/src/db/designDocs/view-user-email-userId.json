{
    "_id": "_design/view-user-email-userId",
    "views": {
        "view-user-email-userId": {
            "map": "function (doc) {  if (doc.type != \"user\") return; if (doc.userId) emit(doc.userId, doc._id); if (doc.email) emit(doc.email, doc._id); }"
        }
    }
}
