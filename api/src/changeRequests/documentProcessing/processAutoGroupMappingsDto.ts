import { AutoGroupMappingsDto } from "../../dto/AutoGroupMappingsDto";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default async function processAutoGroupMappingsDto(doc: AutoGroupMappingsDto) {
    // No singleton enforcement — each mapping is its own document
}
