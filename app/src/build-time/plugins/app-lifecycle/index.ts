/**
 * Bootstrap milestone hooks. The browser build has nothing to do at these
 * points; a packaged-app build swaps this module to hand over from its native
 * launch chrome once the web UI can paint.
 */
export function notifyUiReady(): void {}
